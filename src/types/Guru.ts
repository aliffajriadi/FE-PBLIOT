export interface Teacher {
  id: string;
  nama: string;
  email: string;
  telepon: string;
  mataPelajaran: string[];
  alamat: string;
}

export interface TeacherFormData {
  id: string;
  nama: string;
  email: string;
  telepon: string;
  mataPelajaran: string[];
  alamat: string;
}